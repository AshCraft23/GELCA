{
  "name": "gelca-backend",
  "version": "1.0.0",

  "scripts": {
    // Comando para ejecutar el compilador de TypeScript
    "build": "tsc", 
    
    // Ejecuta "npm run build" antes de "npm start"
    "prestart": "npm run build", 
    
    // Este comando ahora buscará el archivo que fue creado por 'prestart'
    "start": "node dist/server.js" 
  }
}

