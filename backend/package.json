{
  "name": "gelca-backend",
  "version": "1.0.0",
  "scripts": {
    // 1. Comando que compila TypeScript a JavaScript
    "build": "tsc", 
    
    // 2. Este script se ejecuta AUTOMÁTICAMENTE antes de 'start'
    "prestart": "npm run build", 
    
    // 3. Este comando ejecuta el archivo .js COMPILADO
    "start": "node dist/server.js" 
  },
  "dependencies": {
    // ...
  },
  "devDependencies": {
    "typescript": "^5.0.0", 
    // ...
  }
}
